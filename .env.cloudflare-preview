# Build-time vars for `pnpm build:preview` (vite --mode cloudflare-preview). Committed: no secrets here.
# Secrets (CONVEX_DEPLOY_KEY) are set as build variables in the Cloudflare dashboard.
PUBLIC_SITE_URL=https://wayfinder-preview.ethangunter.com/ # trailing slash necessary for CORS :(
PUBLIC_CONVEX_URL=https://pastel-frog-838.convex.cloud
CONVEX_SITE_URL=https://pastel-frog-838.convex.site
