// This tells SvelteKit to prerender all pages that use this layout (which is typically all pages).
// This generates static HTML files for all your routes, which Capacitor bundles.
export const prerender = true;

// Optional: If you want a purely client-side rendered SPA (less SEO friendly, but works well for app-like experiences)
// export const ssr = false;