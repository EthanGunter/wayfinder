/** HTTP action that responds to GET requests with a ping response containing request metadata and timestamp. Used for health checks and testing HTTP routes. */
export { ping } from './http';

/** Default HTTP router with registered routes */
export { default } from './http';
