/** BetterAuth component client for integrating Convex with BetterAuth authentication system */
export { authComponent } from './auth';

/** Factory function that creates a BetterAuth instance configured for Convex backend */
export { createAuth } from './auth';

/** Query to get the currently authenticated user from BetterAuth session */
export { getCurrentUser } from './auth';
