export { default as localAuthProvider } from './BrowserAuthProvider';
export type { 
    User, 
    StoredUser as LocalUserProxy,
    SignInCredentials,
    SignUpDetails,
    UnsubscribeFn
} from './types';