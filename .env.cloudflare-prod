# Build-time vars for `pnpm build` (vite --mode cloudflare-prod). Committed: no secrets here.
# Secrets (CONVEX_DEPLOY_KEY) are set as build variables in the Cloudflare dashboard.
PUBLIC_SITE_URL=https://wayfinder.ethangunter.com/ # trailing slash necessary for CORS :(
PUBLIC_CONVEX_URL=https://healthy-ladybug-413.convex.cloud
CONVEX_SITE_URL=https://healthy-ladybug-413.convex.site
